import React from 'react';
import Header from '../common/header';

var Favs = React.createClass({
  render() {
    return (
      <div>
        <Header/>
        favs
      </div>
    );
  }
});

module.exports = Favs;
